import { PosterBackground } from "components/layouts/poster-background";
import { WelcomeContent } from "modules/welcome-content";

export const Welcome = () => {
  return (
    <PosterBackground>
      <WelcomeContent />
    </PosterBackground>
  );
};
